galaxy_info:
  author: Marco Antonio Tangaro
  description: Install Galaxy workflow manager (production environment) on CentOS7 + postresql, nginx, uwsgi, proftpd
  company: IBBE-CNR
  license: Apache
  min_ansible_version: 2.0
  platforms:
  - name: EL
    versions:
    - 7
  galaxy_tags:
    - galaxy
    - web
dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
