# Add a fast check to speed reconfiguration
- stat: path=/usr/bin/galaxy-startup
  register: galaxy_installed

- include: install.yml
  when: not galaxy_installed.stat.exists

- include: install-ShedTools.yml
  when: (galaxy_installed.stat.exists and galaxy_flavor != "galaxy-no-tools" and galaxy_lrms == "local" and ansible_virtualization_type != "docker")
