---
# Galaxy install tools vars

galaxy_tools_tool_list_file: "{{galaxy_flavor}}-list_file.yml"

galaxy_tools_galaxy_instance_url: "{{galaxy_instance_url}}"

galaxy_tools_api_key: "{{GALAXY_ADMIN_API_KEY}}"

galaxy_tools_ignore_errors: true # should the playbook continue when errors are found
